# AGENTS.md

## このプロジェクトの目的

- 外部AIの会話ログを、そのまま保存するのではなく「再開可能な対話ノート」に変換して残す。
- MVP では、取り込み、整理、再開、ゴミ箱運用を最優先にする。

## 開発ルール

- Next.js App Router + TypeScript + Tailwind CSS を前提にする。
- 永続化は当面 `localStorage` を使うが、画面から直接アクセスせず repository 層を経由する。
- 新機能追加時は MVP 範囲かどうかを先に確認し、過剰実装を避ける。
- UI 変更時は「何を知りたかったか / 何が分かったか / 次に何を聞くべきか」がすぐ見えることを優先する。
- 生成ロジックは LLM 依存にせず、必ずフォールバックを維持する。
- OpenAI API キーはクライアントへ露出させず、必ず Route Handler / サーバー側処理から利用する。
- 生成時の圧縮ロジックは `lib/openai/compression.ts` に集約し、UI にベタ書きしない。

## コードスタイル

- 型定義は `lib/types.ts` を中心に管理する。
- バリデーションは Zod を優先する。
- UI コンポーネントは `components/` に分割し、画面ロジックを抱え込みすぎない。
- ユーティリティや整形処理は `lib/` に寄せる。
- 変更理由が分かりにくい処理だけに短いコメントを入れる。

## 完了条件

- 新規会話を取り込んでノート化し、保存できる。
- 保存済みノートを一覧・詳細・ゴミ箱で操作できる。
- 検索、並び替え、複数選択の一括ゴミ箱移動が機能する。
- README とこのファイルが最新実装に追従している。

## テスト / 動作確認方法

- `npm install`
- `npm run dev`
- `http://localhost:3000` を開く
- サンプルデータが表示されることを確認する
- 取り込み画面から新規ノートを作成する
- 詳細画面でタイトル編集と再開用プロンプトコピーを確認する
- 複数選択でゴミ箱へ送り、ゴミ箱から復元できることを確認する

## Codex に追加依頼するときの前提情報

- 現在の保存先は `localStorage` で、サーバーDBは未導入。
- LLM ノート生成は `app/api/generate-note/route.ts` と `lib/note-generation.ts` が起点。
- 現在の推奨 LLM 起点は `app/api/notes/generate/route.ts` と `lib/openai/` 配下。
- 一覧・詳細・ゴミ箱の状態管理は `providers/notes-provider.tsx` に集約している。
- 将来の優先候補は、Supabase 移行、認証、検索強化、直接連携機能。
